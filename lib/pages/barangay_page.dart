import 'package:flutter/material.dart';

class BarangayPage extends StatelessWidget {
  const BarangayPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Barangay Page"));
  }
}